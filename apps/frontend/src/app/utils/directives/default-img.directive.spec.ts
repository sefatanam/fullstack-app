import { DefaultImgDirective } from './default-img.directive';

describe('DefaultImgDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultImgDirective();
    expect(directive).toBeTruthy();
  });
});
